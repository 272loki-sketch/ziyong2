import { ConfirmButton } from "../../components/kit.tsx";
import type { OutlineProposalView } from "../types.ts";
function list<T>(value: T[] | undefined | null): T[] { return Array.isArray(value) ? value : []; }
function patchSummary(patch: Record<string, unknown> | undefined): string[] { if (!patch) return ["未附带补丁摘要"]; const rows: string[] = []; if (typeof patch.premise === "string") rows.push("更新故事前提"); if (Array.isArray(patch.currentFocus)) rows.push(`调整当前焦点（${patch.currentFocus.length} 项）`); if (patch.alignment) rows.push("更新剧情对齐判断"); if (Array.isArray(patch.collections)) for (const raw of patch.collections as Array<Record<string, unknown>>) rows.push(`${String(raw.collection || "集合")}：新增/修改 ${Array.isArray(raw.upsert) ? raw.upsert.length : 0}，删除 ${Array.isArray(raw.deleteIds) ? raw.deleteIds.length : 0}`); return rows.length ? rows : Object.keys(patch).map((key) => `修改 ${key}`); }
export function ProposalsModule({ proposals, busy, proposalAction }: { proposals: OutlineProposalView[]; busy: boolean; proposalAction: (proposal: OutlineProposalView, accept: boolean) => void }) { return <div className="planning-page"><div className="planning-page-note">提案只有接受后才写入权威大纲；高风险修改必须在此明确确认。</div>{proposals.map((proposal) => { const issues = list(proposal.audit?.issues); const highRisk = proposal.audit?.verdict === "reject" || issues.some((issue) => issue.severity === "error") || /high|高/i.test(proposal.risk || ""); return <article className={`planning-proposal ${highRisk ? "high-risk" : ""}`} key={proposal.id}><header><div><span>PROPOSAL · {proposal.id.slice(0, 8)} · {proposal.status}</span><h3>{proposal.rationale || "大纲修改建议"}</h3></div><b>{highRisk ? "高风险 · 需确认" : proposal.audit?.verdict === "approve" ? "审计通过" : "待审阅"}</b></header><div className="planning-patch">{patchSummary(proposal.patch).map((row) => <span key={row}>{row}</span>)}</div>{proposal.audit?.summary && <p>{proposal.audit.summary}</p>}{issues.map((issue, index) => <div className={`planning-warning ${issue.severity === "error" ? "error" : ""}`} key={`${issue.code}-${index}`}>{issue.message || issue.code}</div>)}<footer><ConfirmButton className="drawer-btn primary" disabled={busy} confirmText={highRisk ? "再次确认高风险修改" : "再次确认接受"} onConfirm={() => void proposalAction(proposal, true)}>接受提案</ConfirmButton><ConfirmButton className="drawer-btn" disabled={busy} confirmText="再次确认拒绝" onConfirm={() => void proposalAction(proposal, false)}>拒绝</ConfirmButton></footer></article>; })}{proposals.length === 0 && <div className="planning-empty">没有待审提案。创作对谈产生的新方案会自动来到这里。</div>}</div>; }
