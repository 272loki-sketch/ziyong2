import type { OutlineViewResponse } from "../types.ts";
function list<T>(value: T[] | undefined | null): T[] { return Array.isArray(value) ? value : []; }
export function CharactersModule({ state }: { state: OutlineViewResponse["state"] }) { return state && <div className="planning-page"><div className="planning-page-note">人物成长是写作方向，不会把角色锁死。要调整时回创作对谈用自然语言讨论。</div><div className="planning-character-grid">{list(state.characterArcs).map((node) => <article className="planning-character" key={node.id}><div className="planning-card-head"><strong>{node.character || node.title}</strong><span>{node.status || "candidate"}</span></div><h4>{node.title}</h4><div className="planning-arc-route"><span><small>FROM</small>{node.from || "未定义"}</span><i>→</i><span><small>TOWARD</small>{node.toward || "未定义"}</span></div>{node.summary && <p>{node.summary}</p>}<div className="planning-rigidity">约束强度：<b>{node.rigidity || "open"}</b></div></article>)}</div>{list(state.characterArcs).length === 0 && <div className="planning-empty">暂无人物成长路线</div>}</div>; }
